<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title> <%= title %> </title>
	<!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" /> -->
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootswatch@5.3.0/dist/quartz/bootstrap.min.css">
	<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet" >
	<style>
		body {
			display: flex;
			flex-direction: column;
			min-height: 100vh;
			margin: 0;
		}

		.container {
			margin-top: 20px;
			flex: 1;
		}

		.container-bg {
			background: rgba(3, 3, 3, 0.7);
  			backdrop-filter: blur(5px);
			margin-bottom: 4%;
  			padding: 20px;
  			border-radius: 8px;
  			box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
		}

		.cart-alert {
        	position: fixed; /* Overlay on top of everything */
        	top: 20px; /* Adjust position */
        	right: 20px;
        	z-index: 1050; /* Ensure it stays above other elements */
        	display: none; /* Hidden initially */
        	width: auto; /* Adjust width */
        	max-width: 300px; /* Prevent too wide alert */
        	box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Soft shadow for better visibility */
    	}

		.custom-red {
			background-color: rgb(243, 93, 77);
		}

		.custom-red:hover {
			background-color: red;
		}

		.container-footer {
			position: relative;
			flex-shrink: 0;
			margin-top: auto;
			text-align: right;
		}

		.container-footer a {
			margin-top: 20px;
			margin-right: 7%;
			margin-left: auto;
		}

		footer {
			margin-top: auto;
		}
	</style>
</head>

<body>

<div id="cart-alert" class="alert cart-alert alert-success alert-dismissible fade show " role="alert" style="display: none;">
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    <i class="fas fa-check-circle"></i> Item successfully added to cart!
</div>
